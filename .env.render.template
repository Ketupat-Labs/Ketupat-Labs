# Environment Variables Template for Render Deployment
# 
# This file lists all the environment variables you need to set in Render Dashboard.
# DO NOT put actual secrets here - this file is safe to commit to Git.
# 
# Copy the values from your .env.docker file when configuring Render.

# ============================================
# SECRETS - Set these in Render Dashboard
# ============================================

APP_KEY=                    # Copy from .env.docker (starts with "base64:")
DB_HOST=                    # Your Aiven MySQL host
DB_USERNAME=                # Your Aiven MySQL username
DB_PASSWORD=                # Your Aiven MySQL password
REVERB_APP_KEY=             # Copy from .env.docker
REVERB_APP_SECRET=          # Copy from .env.docker (Keep this secret!)
REVERB_APP_ID=              # Copy from .env.docker (e.g., compuplay)
REVERB_HOST=0.0.0.0         # Internal host (Leave as 0.0.0.0)
REVERB_PORT=8080            # Internal port (Leave as 8080)
REVERB_SCHEME=http          # Internal scheme (Leave as http)

# Frontend Reverb (VITE)
VITE_REVERB_APP_KEY=        # Same as REVERB_APP_KEY
VITE_REVERB_HOST=           # Your Render URL (e.g., your-app.onrender.com)
VITE_REVERB_PORT=443        # Port for browser (HTTPS)
VITE_REVERB_SCHEME=https    # Scheme for browser (HTTPS)

GOOGLE_SCRIPT_URL=          # Your Google Apps Script Web App URL

# ============================================
# AUTO-CONFIGURED - Render sets these
# ============================================

APP_URL=                    # Render will provide this (e.g., https://your-app.onrender.com)
REDIS_HOST=                 # Auto-linked from Redis service
REDIS_PORT=                 # Auto-linked from Redis service

# ============================================
# ALREADY SET IN render.yaml
# ============================================

# These are already configured in render.yaml, no need to set manually:
# - APP_NAME
# - APP_ENV
# - APP_DEBUG
# - DB_CONNECTION
# - DB_PORT
# - DB_DATABASE
# - CACHE_DRIVER
# - SESSION_DRIVER
# - QUEUE_CONNECTION
# - BROADCAST_CONNECTION
# - MAIL_MAILER
# - MAIL_HOST
# - MAIL_PORT
# - MAIL_ENCRYPTION
# - MAIL_FROM_ADDRESS
# - MAIL_FROM_NAME
